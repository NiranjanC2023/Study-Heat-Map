{
  "name": "focus-flow-extension",
  "private": true,
  "version": "1.3.4",
  "description": "Focus Flow — Chrome extension for focus tracking and distraction blocking",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
